import { ExploreLayout } from "@/components/workbench/explore-layout";

/**
 * Explore workspace — pivot through graph data, schema overview,
 * neighborhood navigation. Route `/explore`.
 */
export default function ExplorePage() {
  return <ExploreLayout />;
}
