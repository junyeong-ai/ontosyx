import { AnalyzeLayout } from "@/components/workbench/analyze-layout";

/**
 * Analyze workspace — chat with the agent, run multi-hop queries,
 * inspect results. Route `/analyze`.
 */
export default function AnalyzePage() {
  return <AnalyzeLayout />;
}
