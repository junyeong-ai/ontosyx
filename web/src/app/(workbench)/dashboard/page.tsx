import { DashboardLayout } from "@/components/workbench/dashboard-layout";

/**
 * Dashboard workspace — pinned widgets, saved reports. Route `/dashboard`.
 */
export default function DashboardPage() {
  return <DashboardLayout />;
}
