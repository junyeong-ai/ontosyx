import { DesignLayout } from "@/components/workbench/design-layout";

/**
 * Design workspace — ontology modelling, project lifecycle, canvas.
 * Lives at `/design` so reloads and shares land on the same surface.
 */
export default function DesignPage() {
  return <DesignLayout />;
}
