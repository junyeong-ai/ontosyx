import { PALETTE_PRIMARY } from "../chart-utils";

/**
 * Node colors by type — uses the shared platform palette.
 * 30 colors ensures no repetition for graphs with many distinct node types.
 */
export const TYPE_COLORS = PALETTE_PRIMARY;

export const DARK_BG = "#18181b"; // zinc-900
export const LIGHT_BG = "#ffffff";

export const DEFAULT_MAX_NODES = 200;
