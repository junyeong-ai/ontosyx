/**
 * Settings pages that benefit from wider content area (max-w-6xl instead of max-w-3xl).
 * These are data-browsing pages with tables, lists, or cards that need more horizontal space.
 */
export const WIDE_SETTINGS_PAGES = new Set([
  "/settings/knowledge",
  "/settings/recipes",
  "/settings/sessions",
  "/settings/reports",
  "/settings/audit",
  "/settings/usage",
  "/settings/lineage",
]);
